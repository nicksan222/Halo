/**
 * Shared role enums for server and testing helpers.
 */
export enum UserRole {
  User = 'user',
  Admin = 'admin'
}

export enum OrganizationRole {
  Member = 'member',
  Admin = 'admin',
  Owner = 'owner'
}
